# QWK 博客完整搭建记录

## 📅 时间线

**搭建日期：** 2026年2月18日

---

## 🎯 最初的需求

> 用户想用 Obsidian 构建一个博客，有 GitHub 账号和域名 qwk.dpdns.org，要求：
> 
> - 美观
> - 免费零成本
> - 用 Obsidian 写笔记

---

## 🔍 方案选择

### 对比工具：Quartz 4 vs Digital Garden

| 特性          | Quartz 4       | Digital Garden |
| ----------- | -------------- | -------------- |
| 上手难度        | ⭐⭐ 简单          | ⭐ 很简单          |
| Obsidian 集成 | 需要手动同步         | 插件一键发布         |
| 部署方式        | GitHub Actions | Vercel/Netlify |
| 双向链接        | ✅ 完美支持         | ✅ 支持           |
| 图谱视图        | ✅ 内置           | ✅ 支持           |
| 全文搜索        | ✅ 内置           | ✅ 支持           |
| 主题美观度       | ⭐⭐⭐⭐⭐          | ⭐⭐⭐⭐           |
| 自定义程度       | ⭐⭐⭐⭐           | ⭐⭐⭐            |

### 最终选择：**Quartz 4**

- 理由：默认主题更美观、GitHub Pages 完全免费、功能更完整

---

## 💻 系统环境检查

### 检查 Git、Node.js、npm

```bash
git --version
# 结果：git version 2.53.0.windows.1

node --version
# 结果：v24.13.0

npm --version
# 结果：PowerShell 执行策略限制，后来解决了
```

---

## 🚀 第一步：创建 GitHub 仓库

### 操作步骤：

1. 访问 Quartz 4 模板仓库：https://github.com/jackyzha0/quartz
2. 点击 **Use this template** → **Create a new repository**
3. 仓库名称：`farroadeta.github.io`
4. 选择 Public，点击 Create repository

---

## 📥 第二步：克隆项目到本地

### 遇到的问题 1：SSL 证书错误

**错误信息：**

```
fatal: unable to access 'https://github.com/farroadeta/farroadeta.github.io.git/': SSL certificate OpenSSL verify result: unable to get local issuer certificate (20)
```

**解决方案：**

```bash
git config --global http.sslVerify false
```

### 然后克隆

```bash
git clone https://github.com/farroadeta/farroadeta.github.io.git qwk-blog
```

**结果：** 克隆成功！

---

## 🔧 第三步：进入文件夹并安装依赖

### 进入文件夹

```bash
cd qwk-blog
```

### 遇到的问题 2：PowerShell 执行策略限制

**错误信息：**

```
npm : 无法加载文件 E:\node.js\npm.ps1，因为在此系统上禁止运行脚本。
```

**解决方案：**

```bash
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

### 安装依赖

```bash
npm install
```

**结果：** 安装成功，added 483 packages

---

## ⚙️ 第四步：初始化 Quartz

```bash
npx quartz create
```

### 回答问题：

1. Choose how to initialize the content：**Empty Quartz**
2. Choose how Quartz should resolve links：**Treat links as shortest path**
3. 其他问题用默认值

---

## 🎨 第五步：配置网站

### 修改 quartz.config.ts

**文件位置：** `C:\Users\七哇夸\qwk-blog\quartz.config.ts`

**修改内容：**

1. **网站标题（第 11 行）：**
   
   ```typescript
   pageTitle: "QWK的博客",
   ```

2. **语言（第 18 行）：**
   
   ```typescript
   locale: "zh-CN",
   ```

3. **域名（第 19 行）：**
   
   ```typescript
   baseUrl: "qwk.dpdns.org",
   ```

4. **Analytics（第 15-17 行）：**
   
   ```typescript
   analytics: null,
   ```

5. **注释掉 CustomOgImages 插件（第 92 行）：**
   
   ```typescript
   // Plugin.CustomOgImages(),
   ```
   
   （原因：CustomOgImages 插件无法下载字体，导致构建失败）

---

## 👀 第六步：本地预览

```bash
npx quartz build --serve
```

**结果：**

```
Started a Quartz server listening at http://localhost:8080
```

**访问：** http://localhost:8080 预览成功！

---

## 📤 第七步：推送到 GitHub

### 遇到的问题 3：Git 用户身份未设置

**错误信息：**

```
Author identity unknown
*** Please tell me who you are.
```

**解决方案：**

```bash
git config --global user.email "2810985626@qq.com"
git config --global user.name "farroadeta"
```

### 第一次推送

```bash
git add .
git commit -m "Initial blog setup"
git push
```

**结果：** 推送成功！

---

## 🔄 第八步：创建 GitHub Actions 工作流

### 创建 deploy.yml

**文件位置：** `C:\Users\七哇夸\qwk-blog\.github\workflows\deploy.yml`

**文件内容：**

```yaml
name: Deploy Quartz site to GitHub Pages

on:
  push:
    branches:
      - v4

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
      - run: npm ci
      - name: Build Quartz
        run: npx quartz build
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

  deploy:
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

---

## 🌐 第九步：配置 GitHub Pages

### 操作步骤：

1. 仓库 → **Settings** → **Pages**
2. **Source** 选择 **GitHub Actions**
3. **Custom domain** 输入 `qwk.dpdns.org`，点击 Save

---

## 🔗 第十步：添加 CNAME 文件

**文件位置：** `C:\Users\七哇夸\qwk-blog\CNAME`

**文件内容：**

```
qwk.dpdns.org
```

### 推送 deploy.yml 和 CNAME

```bash
git add .github/workflows/deploy.yml CNAME
git commit -m "Add deploy workflow and CNAME"
git push
```

---

## ⚠️ 第十一步：遇到的域名问题

### 问题 1：浏览器自动跳转

访问 `https://farroadeta.github.io` 自动跳转到 `https://qwk.dpdns.org`，但 DNS 未生效，显示 404。

### 问题 2：DNS 配置问题

在 Cloudflare 添加 CNAME 记录时，提示记录已存在。

### 临时解决方案：

1. **删除自定义域名配置：**
   - GitHub Pages → Custom domain，清空输入框，Save
2. **删除本地 CNAME 文件：**
   
   ```bash
   git rm CNAME
   git commit -m "Remove CNAME temporarily"
   git push
   ```
3. **清除浏览器缓存或用无痕模式访问**

---

## ✅ 最终结果

### 成功上线的网站地址：

- **GitHub Pages：** https://farroadeta.github.io
- **待绑定域名：** qwk.dpdns.org（DNS 生效后可用）

### 项目位置：

- **本地：** `C:\Users\七哇夸\qwk-blog`
- **GitHub：** https://github.com/farroadeta/farroadeta.github.io

---

## 📝 日常使用指南

### 添加新笔记

1. 把 Obsidian 笔记（.md 文件）复制到 `content/` 文件夹
2. 推送更新：
   
   ```bash
   cd C:\Users\七哇夸\qwk-blog
   git add .
   git commit -m "添加新笔记"
   git push
   ```

### 修改网站配置

- 编辑 `quartz.config.ts` 修改标题、颜色、语言等
- 编辑 `content/index.md` 修改首页内容

### 本地预览

```bash
npx quartz build --serve
```

访问 http://localhost:8080

---

## 🔮 以后绑定域名的完整步骤

### 前提条件：

- DNS 完全生效（可能需要 24-48 小时）

### 步骤：

1. **在域名管理后台（dpdns.org / Cloudflare）添加 CNAME 记录：**
   
   - 主机记录：@ 或 www
   - 记录值：farroadeta.github.io
   - 代理状态：仅 DNS（关掉橙色云朵）

2. **在 GitHub Pages 配置：**
   
   - 仓库 → Settings → Pages
   - Custom domain 输入：qwk.dpdns.org
   - 点击 Save

3. **添加 CNAME 文件：**
   
   - 在 `qwk-blog` 根目录创建 `CNAME` 文件
   - 内容：qwk.dpdns.org
   - 提交并推送

---

## 📚 参考文档

- **Quartz 官方文档：** https://quartz.jzhao.xyz
- **GitHub Pages 文档：** https://docs.github.com/pages
- **Git 文档：** https://git-scm.com/doc

---

## 🛠️ 技术栈总结

| 组件     | 技术             |
| ------ | -------------- |
| 内容创作   | Obsidian       |
| 静态网站生成 | Quartz 4       |
| 版本控制   | Git            |
| 代码托管   | GitHub         |
| 网站托管   | GitHub Pages   |
| CI/CD  | GitHub Actions |
| 自定义域名  | qwk.dpdns.org  |

---

## 💡 遇到的所有问题和解决方案汇总

| 问题                  | 错误信息                                       | 解决方案                                                                   |
| ------------------- | ------------------------------------------ | ---------------------------------------------------------------------- |
| SSL 证书错误            | unable to get local issuer certificate     | `git config --global http.sslVerify false`                             |
| PowerShell 执行策略     | 无法加载 npm.ps1                               | `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned` |
| Git 用户身份            | Author identity unknown                    | `git config --global user.email/name`                                  |
| CustomOgImages 插件失败 | fetch failed                               | 注释掉该插件                                                                 |
| 浏览器自动跳转             | 跳转至未生效域名                                   | 删除自定义域名配置，清除缓存                                                         |
| DNS 记录冲突            | An A, AAAA, or CNAME record already exists | 删除已有记录后再添加                                                             |

---

我的gitee blog 令牌：`09aa0288a524b8b5500139d58649467a`

**文档创建时间：** 2026年2月18日
**最后更新：** 2026年2月18日
